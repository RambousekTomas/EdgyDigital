import React from 'react'
import { SafeAreaView, StyleSheet } from 'react-native'

const App = () => {
  return <SafeAreaView style={styles.flex}></SafeAreaView>
}

export default App

const styles = StyleSheet.create({
  flex: { flex: 1 },
})
